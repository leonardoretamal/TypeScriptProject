
const name: string = "Strider";

let hpPoint: number | 'FULL' = 95;
const isAlive: boolean = true;


hpPoint = 'FULL';

console.log({ name, hpPoint, isAlive });

export {}; // esto es para vite es un modulo
